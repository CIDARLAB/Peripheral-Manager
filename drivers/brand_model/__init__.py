# This is where the base code will be housed

# TODO - Diana, list all the standard functions we will need for each kind of a standard command


def command1(arg1, arg2) -> bytearray:
    return bytearray(10)

def command2(arg1, arg2) -> bytearray:
    return bytearray(10)

def command2(arg1, arg2) -> bytearray:
    return bytearray(10)

def command3(arg1, arg2) -> bytearray:
    return bytearray(10)

def command4(arg1, arg2) -> bytearray:
    return bytearray(10)
